---
import site from '@data/site.config';
import Section from './Section';
---
<Section client:visible id="education" title="Education">
  <div class="grid gap-4 sm:gap-5">
    {site.education.map((ed) => (
      <div class="card p-4 sm:p-6 hover:shadow-xl transition-shadow">
        <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-1 sm:gap-2">
          <h3 class="text-base sm:text-lg font-semibold break-words">🎓 {ed.degree} <span class="opacity-70">·</span> {ed.institution}</h3>
          <p class="text-xs sm:text-sm opacity-70 whitespace-nowrap flex-shrink-0">📅 {ed.startDate} – {ed.endDate || 'Present'}</p>
        </div>
        {ed.fieldOfStudy && <p class="text-xs sm:text-sm opacity-80 mt-2 break-words">📚 {ed.fieldOfStudy}</p>}
        {ed.gpa && <p class="text-xs sm:text-sm opacity-70 mt-1">🎯 GPA: {ed.gpa}</p>}
        {ed.coursework && (
          <p class="text-xs sm:text-sm opacity-80 mt-3 break-words"><strong>Coursework:</strong> {ed.coursework.join(', ')}</p>
        )}
        {ed.honors && ed.honors.length > 0 && (
          <p class="text-xs sm:text-sm opacity-80 mt-2 break-words">🏆 <strong>Honors:</strong> {ed.honors.join(', ')}</p>
        )}
      </div>
    ))}
  </div>
</Section>
